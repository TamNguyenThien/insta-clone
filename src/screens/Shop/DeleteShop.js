import React from 'react'
import {Text, StyleSheet, View, TouchableOpacity} from 'react-native'

export default function DeleteMenu({navigation}) {

	return (
    <View>
      
    </View>
	)
}

const styles = StyleSheet.create({

})
